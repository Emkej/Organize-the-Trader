# Agent Notes

## Commit Message Style

- Use Conventional Commits: `<type>: <subject>`.
- Keep the `type` lowercase (for example: `fix`, `feat`, `docs`).
- Capitalize only the first character of the first word in the subject.
- Keep the subject imperative and concise.

Example:
- `fix: Package from local workspace with deploy fallback`
